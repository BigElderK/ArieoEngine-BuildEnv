# @package_name@ CMake configuration file
# Generated automatically by CMake

@PACKAGE_INIT@

# Find required dependencies
include(CMakeFindDependencyMacro)
@package_dependencies_code@

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/@package_name@Targets.cmake")

# Set variables for compatibility with older find_package() code
set(@package_name@_FOUND TRUE)
set(@package_name@_INCLUDE_DIRS "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@")
set(@package_name@_LIBRARIES @package_targets_list@)

# Check required components
check_required_components(@package_name@)

if(NOT @package_name@_FIND_QUIETLY)
    message(STATUS "Found @package_name@: ${CMAKE_CURRENT_LIST_DIR}")
endif()
