{
    "name": "Arieo-BuildEnv",
    "description": "Build environment for Areio projects",
    "build_commands": 
    [
        "${CMAKE_COMMAND} --build ."        
    ],
    "install_commands": 
    [
        "${CMAKE_COMMAND} --build . --target install"
    ]
}